# NozzleCleanerProject

How wo use this code is the same as in the main just put the path of picture and best.pt in this function

dowload all of this file and put it in the same folder

detect_blocked_nozzles(YOUR PICTURE PATH) -> LIST OF BLOCKED HOLE

necessary : Python 3.10 Up
# Core AI & Computer Vision
ultralytics==8.2.103      # YOLOv8
torch==2.3.1+cpu          # ถ้าไม่มี CUDA (ถ้ามี GPU ใช้เวอร์ชันที่ตรงกับ CUDA)
torchvision==0.18.1+cpu
torchaudio==2.3.1+cpu

opencv-python==4.10.0.84  # OpenCV สำหรับประมวลผลภาพ
numpy==1.26.4             # คณิตศาสตร์พื้นฐาน
matplotlib                # ใช้บ้างกรณี debug

# Optional tools
tqdm                      # แสดง progress bar
pillow                    # จัดการไฟล์ภาพ

